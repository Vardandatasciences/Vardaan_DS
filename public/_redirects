# Redirects for Netlify SPA routing
# This ensures that all routes are handled by React Router

# API routes should be handled by backend - update with your backend URL
/api/*  https://your-backend-domain.com/api/:splat  200

# All other routes should serve the React app
/*    /index.html   200 